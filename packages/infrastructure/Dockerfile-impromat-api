FROM node:16-alpine

ARG GOOGLE_AUTH_JSON_PATH
ENV GOOGLE_AUTH_JSON_PATH ${GOOGLE_AUTH_JSON_PATH}
ARG STORAGE_PATH
ENV STORAGE_PATH ${STORAGE_PATH}

WORKDIR /app
COPY . .

RUN yarn install --frozen-lockfile

WORKDIR /app/packages/impromat-api
CMD ["yarn", "start"]
