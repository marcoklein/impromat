{
  "name": "impromat-monorepo",
  "version": "32",
  "private": true,
  "type": "module",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "@changesets/cli": "^2.25.2"
  },
  "scripts": {
    "dev": "yarn foreach --from '{impromat,impromat-api}' run dev",
    "lint": "yarn foreach run lint",
    "test": "yarn foreach run test",
    "build": "yarn foreach run build",
    "docker-compose": "yarn workspace infrastructure docker-compose",
    "infra:config": "./packages/infrastructure/configure.sh",
    "foreach": "yarn workspaces foreach --parallel --verbose --interlaced --exclude impromat-monorepo"
  },
  "description": "Impromat Monorepo",
  "main": "index.js",
  "author": "Marco Klein <hello@marcoklein.dev>",
  "license": "MIT",
  "packageManager": "yarn@3.4.1"
}
